// Generated by CoffeeScript 1.12.7
(function() {
  var bindRowListeners, currentPage, escapeChars, filteredTags, loadItems, packFilterParam, urlObject, urlString;

  currentPage = 0;

  filteredTags = [];

  urlString = window.location.href;

  urlObject = new URL(urlString);

  this.server = urlObject.searchParams.get('server');

  this.port = urlObject.searchParams.get('port');

  loadItems = function(payload, container, btnPre, btnSuc, btnFilter, pageIndicator, inputTags) {
    pageIndicator.closest('div').css('visibility', 'hidden');
    return listItems(payload, function(result) {
      var i, item, len, ref;
      if (result['stat'] === Status.COMPLETE) {
        currentPage = result['obj']['currentPage'];
        btnPre.attr('disabled', result['obj']['isFirst']);
        btnSuc.attr('disabled', result['obj']['isLast']);
        pageIndicator.text(currentPage);
        container.empty();
        ref = result['obj']['items'];
        for (i = 0, len = ref.length; i < len; i++) {
          item = ref[i];
          container.append(generateRow(item));
        }
        bindRowListeners(inputTags, btnFilter);
        window.scrollTo(0, 0);
      } else {
        if (result['stat'] === Status.ERROR) {
          btnPre.attr('disabled', true);
          btnSuc.attr('disabled', true);
          container.empty();
          container.append("<tr style='font-size: 15px'><td colspan='4'>无结果</td></tr>");
        }
      }
      return pageIndicator.closest('div').css('visibility', 'visible');
    });
  };

  bindRowListeners = function(inputTags, btnFilter) {
    $('.editHref').click(function(e) {
      e.preventDefault();
      showEditModal(this);
      return false;
    });
    return $('.tagHref').click(function() {
      inputTags.val($(this.childNodes[0]).text());
      return btnFilter.trigger('click');
    });
  };

  packFilterParam = function(rawVal, page) {
    filteredTags = rawVal.replace(/[， 、]/g, ',').split(',').map(function(e) {
      return e.trim();
    }).filter(function(e) {
      return e !== '';
    }).map(function(e) {
      return escape(e);
    });
    return {
      page: page,
      tag: filteredTags
    };
  };

  $(function() {
    var btnClear, btnFilter, btnPre, btnSuc, container, inputTags, pageIndicator;
    container = $('#tableBody');
    btnPre = $('.btnPre');
    btnSuc = $('.btnSuc');
    pageIndicator = $('.pageIndicator');
    btnFilter = $('#btnFilter');
    btnClear = $('#btnClear');
    inputTags = $('#filterByTags');
    btnPre.click(function() {
      return loadItems(packFilterParam(filteredTags, currentPage - 2), container, btnPre, btnSuc, btnFilter, pageIndicator, inputTags);
    });
    btnSuc.click(function() {
      return loadItems(packFilterParam(filteredTags, currentPage), container, btnPre, btnSuc, btnFilter, pageIndicator, inputTags);
    });
    btnFilter.click(function() {
      return loadItems(packFilterParam(inputTags.val(), 0), container, btnPre, btnSuc, btnFilter, pageIndicator, inputTags);
    });
    btnClear.click(function() {
      inputTags.val('');
      return btnFilter.trigger('click');
    });
    inputTags.val('');
    loadItems({
      page: 0
    }, container, btnPre, btnSuc, btnFilter, pageIndicator, inputTags);
    return getTags(function(result) {
      return new Awesomplete(document.getElementById('filterByTags'), {
        list: result['obj'].map(function(e) {
          return escapeChars(unescape(e.trim()));
        }),
        filter: function(text, input) {
          return Awesomplete.FILTER_CONTAINS(text, input.match(/[^,]*$/)[0]);
        },
        item: function(text, input) {
          return Awesomplete.ITEM(text, input.match(/[^,]*$/)[0]);
        },
        replace: function(text) {
          var before;
          before = this.input.value.match(/^.+,\s*|/)[0];
          return this.input.value = before + text + ", ";
        },
        minChars: 1,
        maxItems: 5,
        autoFirst: true
      });
    });
  });

  escapeChars = function(string) {
    return string.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
  };

  this.generateRow = function(item) {
    var tagsBadges;
    tagsBadges = item['tags'].map(function(e) {
      return "<a class='tagHref' href='#'><span class='badge badge-dark' style='font-size: 11px; vertical-align: middle'>" + (escapeChars(unescape(e['name']))) + "</span></a>";
    }).join('&nbsp;');
    return "<tr><td style='vertical-align: middle'>" + item['id'] + "</td><td style='vertical-align: middle; font-weight: bold; font-size: 15px'><a href='" + item['url'] + "'>" + (escapeChars(unescape(item['title']))) + "</a></td><td style='vertical-align: middle'>" + tagsBadges + "</td><td style='font-size: 15px; vertical-align: middle'><a href='#' class='editHref'><i class='fa fa-pencil fa-1'></i>&nbsp;编辑</a></td></tr>";
  };

}).call(this);
