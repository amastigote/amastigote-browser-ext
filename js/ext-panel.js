// Generated by CoffeeScript 1.12.7
(function() {
  var add_btn, browse_btn, categoryContainer, collectItem, delete_btn, escapeChars, img_settings, loadCategories, mask, settings_btn, tag_input, title_input, updateIcon, updatePanel, update_btn, url_input;

  add_btn = $('#btn_add');

  update_btn = $('#btn_update');

  delete_btn = $('#btn_delete');

  browse_btn = $('#img_page');

  settings_btn = $('#btn_settings');

  title_input = $('#input_title');

  url_input = $('#input_url');

  tag_input = $('#input_tag');

  img_settings = $('#img_settings');

  categoryContainer = $('#select_category');

  mask = $('#mask');

  mask.css('height', $(document).height());

  mask.css('width', $(document).width());

  browser.storage.local.get('tags').then(function(result) {
    var extractLast, split;
    if (result['tags'] === void 0) {
      result = {
        tags: []
      };
    }
    split = function(val) {
      return val.split(/,\s*/);
    };
    extractLast = function(term) {
      return split(term).pop();
    };
    return $('#input_tag').autocomplete({
      minLength: 1,
      source: function(request, response) {
        return response($.ui.autocomplete.filter(result['tags'].map(function(e) {
          return escapeChars(unescape(e.trim()));
        }), extractLast(request.term)));
      }
    }, {
      focus: function() {
        return false;
      },
      select: function(event, ui) {
        var terms;
        terms = split(this.value);
        terms.pop();
        terms.push(ui.item.value);
        terms.push("");
        this.value = terms.join(", ");
        return false;
      }
    });
  });

  browse_btn.click(function() {
    return browser.storage.local.get([__serverKey, __portKey]).then(function(result) {
      browser.tabs.create({
        'url': '/html/page.html?server=' + escape(result[__serverKey]) + '&port=' + escape(result[__portKey])
      });
      return window.close();
    });
  });

  add_btn.click(function() {
    mask.fadeIn();
    add_btn.prop('disabled', true);
    return create(collectItem(), function(result) {
      mask.fadeOut();
      if (result['stat'] === Status.COMPLETE) {
        update_btn.prop('disabled', false);
        delete_btn.prop('disabled', false);
        return browser.tabs.query({
          currentWindow: true,
          active: true
        }).then(function(tabs) {
          if (tabs[0]) {
            return updateIcon(true, tabs[0].id);
          }
        });
      } else {
        return add_btn.prop('disabled', false);
      }
    });
  });

  update_btn.click(function() {
    mask.fadeIn();
    update_btn.prop('disabled', true);
    return update(collectItem(), function(result) {
      updatePanel(result);
      mask.fadeOut();
      return update_btn.prop('disabled', false);
    });
  });

  delete_btn.click(function() {
    mask.fadeIn();
    delete_btn.prop('disabled', true);
    return remove(collectItem(), function() {
      mask.fadeOut();
      add_btn.prop('disabled', false);
      update_btn.prop('disabled', true);
      return browser.tabs.query({
        currentWindow: true,
        active: true
      }).then(function(tabs) {
        if (tabs[0]) {
          return updateIcon(false, tabs[0].id);
        }
      });
    });
  });

  img_settings.click(function() {
    browser.runtime.openOptionsPage();
    return window.close();
  });

  settings_btn.click(function() {
    browser.runtime.openOptionsPage();
    return window.close();
  });

  updateIcon = function(hasColor, tabId) {
    return browser.browserAction.setIcon({
      path: hasColor ? {
        48: '../pic/cn_1.png'
      } : {
        48: '../pic/cn_0.png'
      },
      tabId: tabId
    });
  };

  collectItem = function() {
    return {
      title: escape(title_input.val()),
      url: url_input.val(),
      tags: tag_input.val().replace(/[， 、]/g, ',').split(',').map(function(e) {
        return e.trim();
      }).filter(function(e) {
        return e !== '';
      }).map(function(e) {
        return escape(e);
      }),
      categoryName: escape(categoryContainer.children(":selected").text())
    };
  };

  escapeChars = function(string) {
    return string.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
  };

  browser.tabs.query({
    currentWindow: true,
    active: true
  }).then(function(tabs) {
    var url;
    if (tabs[0]) {
      url = tabs[0].url;
      title_input.val(removeSuffix(url, tabs[0].title));
      url_input.val(url);
      return get_item({
        url: url_input.val()
      }, function(result) {
        var thisCategory;
        if (result['stat'] === Status.COMPLETE) {
          add_btn.prop('disabled', true);
          updatePanel(result);
          thisCategory = result['obj']['category']['name'];
          return loadCategories(categoryContainer, thisCategory);
        } else if (result['stat'] === Status.ERROR) {
          update_btn.prop('disabled', true);
          delete_btn.prop('disabled', true);
          return loadCategories(categoryContainer);
        }
      });
    }
  });

  updatePanel = function(result) {
    title_input.val(escapeChars(unescape(result['obj']['title'])));
    return tag_input.val(result['obj']['tags'].map(function(e) {
      return escapeChars(unescape(e.name));
    }).join(', '));
  };

  loadCategories = function(container, thisCategory) {
    return get_category(function(result) {
      var i, item, itemHtml, len, ref;
      ref = result['obj'];
      for (i = 0, len = ref.length; i < len; i++) {
        item = ref[i];
        itemHtml = "<option>" + (unescape(item['name'])) + "</option>";
        if (item['name'] === thisCategory) {
          itemHtml = $($.parseHTML(itemHtml)).prop('selected', 'selected');
        }
        container.append(itemHtml);
      }
      return mask.fadeOut();
    });
  };

}).call(this);
